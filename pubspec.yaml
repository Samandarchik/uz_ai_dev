name: uz_ai_dev
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  dio: ^5.9.0
  easy_localization: ^3.0.8
  flutter:
    sdk: flutter
  get_it: ^8.2.0
  http: ^1.5.0
  shared_preferences: ^2.5.3
  talker_dio_logger: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/translations/

# flutter_icons:
#   android: true
#   ios: true
#   image_path: "lib/ui/screens/image.png"
